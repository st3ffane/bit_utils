{
  "name": "bit-utils",
  "version": "1.0.0",
  "description": "binary operations utility",
  "main": "build/bit.utils.min.js",
  "scripts": {
    "test": "nyc --bail --exit --all mocha",
    "start:dev": "NODE_ENV=development && node index.js",
    "start": "NODE_ENV=production && node index.js"    
  },
  "keywords": [],
  "author": "",
  "license": "Beerware",
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint-config-google": "^0.9.1",
    "mocha": "^5.1.1",
    "nyc": "^11.7.1"
  }
}
